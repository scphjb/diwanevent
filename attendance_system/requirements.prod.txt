# ══════════════════════════════════════════════
#  Diwan Event Platform — Production Dependencies
#  مكتبات إضافية مطلوبة لبيئة التشغيل والإنتاج
# ══════════════════════════════════════════════

# خادم الويب للإنتاج وإدارة خلايا العمل
gunicorn==22.0.0

# طابور المهام في الخلفية
celery==5.4.0
redis>=5.0.4

# مراقبة الأداء وتتبع طوابير Celery (اختياري، مفيد للإنتاج)
flower==2.0.1

# التحقق من اتصالات قاعدة البيانات
psycopg2-binary==2.9.11
