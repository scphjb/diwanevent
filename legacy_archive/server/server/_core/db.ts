/**
 * Mock Database Client (Prisma)
 * This is a placeholder since the actual Prisma setup is missing.
 */
export const prisma: any = {
  webhookEvent: {
    findMany: async () => [],
    update: async () => ({}),
  }
};
